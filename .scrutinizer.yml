filter:
  excluded_paths:
    - 'tests/*'
checks:
  php: true
build:
  environment:
    php: '5.6'
    postgresql: false
  tests:
    override:
      -
        command: 'vendor/bin/phpunit --coverage-clover=.coverage.tmp'
        coverage:
          file: '.coverage.tmp'
          format: 'clover'
coding_style:
  php:
    spaces:
      other:
        after_type_cast: false